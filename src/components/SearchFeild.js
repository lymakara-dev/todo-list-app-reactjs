import React from "react";

const SearchFeild = () => {
  return (
    <div>
      <input type="text" placeholder="Search taks here."></input>
      <button>Find</button>
    </div>
  );
};

export default SearchFeild;
